# 原有绘图示例目录当前不使用

原有绘图示例目录已退出正式工作流。`recipes_common_50/` 与 `recipes_advanced/` 中的文件尚未完成来源、许可证、依赖和统计方法核查，不得在任务中检索、执行、复制或推荐。

重新使用单个示例时按 [recipe-quarantine.md](recipe-quarantine.md) 核查，并只把通过核查的最小通用实现写入正式 reference 或 script；不恢复整套示例目录的自动调用。
